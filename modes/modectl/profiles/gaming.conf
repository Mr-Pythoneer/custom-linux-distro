# Gaming mode profile — sourced by distro-modectl.
MODE_NAME="Gaming"
CPU_GOVERNOR="performance"
POWER_PROFILE="performance"
ENABLE_SERVICES=()
DISABLE_SERVICES=()
STOP_AI_PRESET=true
CONFIRM_DISPLAY_MANAGER_STOP=false
PINNED_APPS=(steam.desktop lutris.desktop com.usebottles.bottles.desktop)
NOTES="GameMode is invoked per-launch by Steam/Lutris (gamemoderun), not run as a system service — nothing to enable here. PINNED_APPS desktop-file IDs are best-guess, unverified against a real install (see modes/gaming/setup/verify-gaming.sh to confirm actual installed app IDs with: ls /usr/share/applications, flatpak list --app)."
