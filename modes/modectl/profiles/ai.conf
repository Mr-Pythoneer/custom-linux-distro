# AI mode profile — sourced by distro-modectl.
MODE_NAME="AI"
CPU_GOVERNOR="schedutil"
POWER_PROFILE="balanced"
ENABLE_SERVICES=()
DISABLE_SERVICES=()
STOP_AI_PRESET=false
AI_AUTOSTART_PRESET="crucible"
CONFIRM_DISPLAY_MANAGER_STOP=false
PINNED_APPS=()
NOTES="Starts distro-ai-preset switch \$AI_AUTOSTART_PRESET on entry — see modes/ai/. Requires distro-ai-preset to be installed (modes/ai/bin). OpenCode is a terminal CLI, not a desktop app, so nothing to pin here yet -- a terminal-launcher pin could be added once Normal mode's dock (see modes/normal/) actually exists."
