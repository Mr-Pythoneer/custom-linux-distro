# AI mode profile — sourced by distro-modectl.
MODE_NAME="AI"
CPU_GOVERNOR="schedutil"
POWER_PROFILE="balanced"
ENABLE_SERVICES=()
DISABLE_SERVICES=()
STOP_AI_MODEL=false
AI_AUTOSTART_USECASE="coding"
CONFIRM_DISPLAY_MANAGER_STOP=false
GPU_PERF=auto
PINNED_APPS=()
NOTES="Loads the LM Studio model for use-case \$AI_AUTOSTART_USECASE on entry (default: coding -> Qwen2.5-Coder-32B) via distro-ai-model, and ensures the OpenAI server is up on :8080. Requires modes/ai/setup/ to have installed LM Studio + pulled models. Change AI_AUTOSTART_USECASE to any use-case in modes/ai/config/models.catalog.json (coding/cad/day-to-day/uncensored/assistant/vision)."
