{
  "$comment": "Points OpenCode at the local LM Studio OpenAI-compatible server (port 8080). Load a model first with: distro-ai-model use coding. The model id below matches the --identifier distro-ai-model sets when loading. Copy this to ./opencode.json in your project, or use distro-ai-cloud-toggle to switch between this and Claude (cloud).",
  "$schema": "https://opencode.ai/config.json",
  "provider": {
    "lmstudio": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "LM Studio (local, port 8080)",
      "options": {
        "baseURL": "http://127.0.0.1:8080/v1",
        "apiKey": "lm-studio"
      },
      "models": {
        "qwen2.5-coder-32b": { "name": "Qwen2.5-Coder-32B (LM Studio)" },
        "qwen2.5-coder-7b": { "name": "Qwen2.5-Coder-7B (LM Studio)" }
      }
    }
  }
}
